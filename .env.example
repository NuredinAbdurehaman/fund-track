# PostgreSQL connection for Prisma (Phase 3)
DATABASE_URL="postgresql://user:password@localhost:5432/fund_track?schema=public"

# Set to "true" when DATABASE_URL is configured and migrations are applied
NEXT_PUBLIC_USE_API="false"
